<div class="jumbotron">
  <h2 class="h1">
    Projects
  </h2>

  <div class="projects--list">

    {% for project in site.data.projects %}
      {% if project.featured %}
        {% render "projects/_project" project: project %}
      {% endif %}
    {% endfor %}
  </div>
</div>
