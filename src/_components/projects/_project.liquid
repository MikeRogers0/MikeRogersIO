<div class="rounded shadow-lg overflow-hidden flex flex-wrap flex-col sm:flex-row lg:flex-col">
  <a class="flex-1 bg-gray-100 p-2 block flex items-center" href="{{ project.url }}">
    <img class="w-auto mx-auto rounded" src="/images/{{ project.image_path }}" alt="{{ project.name }} Logo" style="max-height: 12rem;">
  </a>
  <div class="flex-1 bg-white p-6 flex flex-col justify-between">
    <div class="flex-1">
      <h3 class="text-xl font-semibold text-gray-900">
        {{ project.name }}
      </h3>
      <p class="mt-3 text-base text-gray-500">
        {{ project.description }}
      </p>
    </div>
    <div class="mt-6 flex flex-wrap items-center justify-between">
      <a href="{{ project.url }}" class="bg-gray-900 text-gray-100 px-5 py-3 font-semibold rounded no-underline">Visit</a>
      <a href="#" class="bg-white text-gray-900 py-3 px-5 rounded border border-solid no-underline hover:underline">Learn More</a>
    </div>
  </div>
</div>
